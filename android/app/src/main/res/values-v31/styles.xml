<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ 系統 SplashScreen API：純色背景 + 中央 icon -->
    <style name="LaunchTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">@drawable/launch_background</item>
        <!-- 無邊框(edge-to-edge),同 values/styles.xml 的說明。 -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:forceDarkAllowed">false</item>
        <item name="android:windowFullscreen">false</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowSplashScreenBackground">#FFFFFF</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/android12splash</item>
    </style>
    <style name="NormalTheme" parent="@android:style/Theme.Light.NoTitleBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <!-- 無邊框(edge-to-edge):Flutter 起動後會把 Activity theme 由 LaunchTheme
             換成 NormalTheme,所以同一組屬性這裡必須再設一次。parent 的舊版
             Theme.Light/Black.NoTitleBar 預設 windowDrawsSystemBarBackgrounds=false,
             少了它 Android 11~14(引擎只在 API<30 自行補旗標)的
             setStatusBarColor/setNavigationBarColor 會失效、系統列維持不透明。 -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
    </style>
</resources>
